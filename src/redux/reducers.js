/*
* 根据 provState  和action
* 生成newState
* */
import {combineReducers} from "redux";

function xxx(provState={},action) {
    switch (action.type) {
        default : return provState
    }
}

export default combineReducers({
    xxx
})
